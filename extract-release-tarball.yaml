---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: busybox

inputs:
- name: release

outputs:
- name: webapp-source

run:
  path: tar
  args: ['zxvf', 'release/source.tar.gz', '-C', 'webapp-source/', '--strip-components', '1']
