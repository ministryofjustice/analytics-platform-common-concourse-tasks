platform: linux
image_resource:
  type: docker-image
  source:
    repository: python
    tag: alpine
inputs:
- name: source
- name: common-tasks
run:
  path: sh
  args:
  - -ec
  - |
    pip install flake8 flake8-quotes
    flake8 --config=./common-tasks/conf/.flake8 ./source/
